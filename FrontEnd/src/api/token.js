// export const getUserFromToken = async (token) => {
//     try {
//         // Aquí puedes usar el token para realizar alguna lógica, como decodificarlo para obtener información del usuario
//         // eslint-disable-next-line no-undef
//         const user = await decodeTokenAndGetUserData(token); // Suponiendo que hay una función para decodificar el token y obtener los datos del usuario
//         return user;
//     } catch (error) {
//         throw new Error("Error fetching user data from token");
//     }
// };
